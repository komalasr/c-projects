#include <stdio.h>

void sumof2() {
    int a, b, sum;

    // Input two numbers
    printf("Enter two integers: ");
    scanf("%d %d", &a, &b);

    // Calculate sum
    sum = a + b;

    // Display result
    printf("Sum of %d and %d is %d\n", a, b, sum);

 //   return 0;
}
